{
  "name": "maintenance-app",
  "version": "1.0.0",
  "description": "A preventive maintenance checklist application.",
  "scripts": {
    "build:css": "node -e \"require('fs').mkdirSync('public',{recursive:true})\" && tailwindcss -i ./src/input.css -o ./public/styles.css --minify",
    "build": "npm run build:css"
  },
  "keywords": [
    "maintenance",
    "checklist",
    "google-sheets"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.10",
    "postcss": "^8.4.41",
    "autoprefixer": "^10.4.20"
  }
}
