{
  "name": "maintenance-checklist-app",
  "version": "1.0.0",
  "description": "A serverless app for a maintenance checklist.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "googleapis": "^126.0.1"
  }
}

